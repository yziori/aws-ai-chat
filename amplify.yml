version: 1
applications:
  - frontend:
      phases:
        build:
          commands:
            - 'pnpm run build'
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - 'node_modules/**/*'